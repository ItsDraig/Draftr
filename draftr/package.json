{
  "name": "draftr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":         "concurrently -n backend,frontend -c blue,magenta \"cd backend && venv\\Scripts\\uvicorn main:app --reload --port 3001\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix frontend && npm install",
    "build":       "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}